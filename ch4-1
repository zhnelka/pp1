//A. Кролики
#include <iostream>

using namespace std;

int main() {
    long long n, m;
    cin >> n >> m;

    long long maxRabbits = (m + n - 1) / n; 
    cout << maxRabbits << endl;

    return 0;
}

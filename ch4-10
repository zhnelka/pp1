//E. Встречалось ли число раньше
#include <iostream>
#include <sstream>
#include <set>
#include <string>

using namespace std;

int main() {
    string input;
    getline(cin, input);
    stringstream ss(input);
    set<int> seen;
    int num;

    while (ss >> num) {
        if (seen.count(num)) {
            cout << "YES" << endl;
        } else {
            cout << "NO" << endl;
            seen.insert(num);
        }
    }

    return 0;
}
